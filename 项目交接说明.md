# 济南水务 Home Assistant 集成 + 前端卡片 —— 项目交接说明

> 本文档用于**新开对话窗口时快速恢复全部上下文**。读完这一份即可继续开发，无需回溯历史聊天。
> 最后更新：2026-09-13　卡片版本 `v1.1.2`

---

## 0. 一句话概览

把济南水务微信小程序的接口封装成 **Home Assistant 自定义集成（HACS）**，并配套写了一个**零依赖的 Lovelace 前端卡片**展示水费/用水数据。
仓库根目录 `E:\workspace\JinanWaterHA` 既是集成仓库，也存放卡片 JS 与本地预览页。

---

## 1. ⚠️ 开发约定（用户明确要求，必须遵守）

1. **涉及接口开发需要读取 `接口.md`**，严格遵循其中约定的接口方式、字段名、实体命名。
2. **遇到不确定的点必须先问用户确认**（例如：多户号下实体范围、命名冲突、字段含义不明），确认后再动手。
3. **接口调用方式**：请求 header 复用 `__init__.py` 的 `_get_auth_headers()`，只替换请求 url 和 body。
4. `smart-utilities-card-ele.js`（旧的国家电网电卡，760KB）**保留不动，仅供回顾学习**。

---

## 2. 仓库结构

```
E:\workspace\JinanWaterHA\
├── custom_components\jinan_water\      ← HA 集成本体
│   ├── __init__.py        协调器 + 集成生命周期（数据编排 6 步）
│   ├── api.py             所有 HTTP 请求逻辑（认证头/请求/字段映射）
│   ├── sensor.py          21 个传感器类
│   ├── button.py          1 个"刷新数据"按钮
│   ├── helpers.py         build_device_info() 共享函数
│   ├── frontend.py        卡片前端资源自动注册（静态路径 + Lovelace 资源表）
│   ├── www\jinan-water-card.js   前端卡片（随集成分发）
│   ├── const.py           全部常量（DOMAIN / 配置字段 / API 端点 / 持久化参数）
│   ├── history.py         日用水历史持久化（HA 原生 Store，落盘 .storage/jinan_water/daily_history.json）
│   ├── config_flow.py     配置流 + 选项流
│   ├── manifest.json      HACS 集成清单
│   ├── strings.json / translations\zh-Hans.json   翻译
│   ├── services.yaml      服务定义
│   └── brand\             icon.png / logo.png
├── 需求.md                             ← 需求与接口约定（开发前必读）
├── 项目交接说明.md                     ← 本文档
├── hacs.json / LICENSE / README.md
└── .workbuddy\memory\                 ← 项目记忆（按日期的工作日志 + 长期笔记）
```

---

## 3. 后端集成（`custom_components/jinan_water`）

### 3.1 模块职责（重构后）

| 模块 | 职责 |
|---|---|
| `const.py` | 所有常量。**不写业务逻辑。** |
| `api.py` | `JinanWaterApi` 类：`_get_auth_headers()`、`_call_api()`、`fetch_account_list()`、`fetch_invoice_list()`、`fetch_yibiao_data()`、`transform_invoice_records()`，以及字段映射 `INVOICE_FIELD_MAP` / `INVOICE_MX_FIELD_MAP`。**所有接口请求逻辑都在这里。** |
| `helpers.py` | `build_device_info(entry, gs, data)`，sensor.py 与 button.py 共用构造 `_attr_device_info`。**不得再复制粘贴字典。** |
| `frontend.py` | **卡片前端资源生命周期**：注册 = `async_setup_card()`（静态路径 + 写资源表，失败回退 `frontend.add_extra_js_url()`）；移除 = `async_remove_card()`（卸载集成时删资源）。公共取表逻辑在 `_async_get_resources()`。**改卡片部署方式只看这个文件。** |
| `__init__.py` | 仅集成生命周期 + `JinanWaterCoordinator.async_update_data()` 的数据编排；HTTP 细节委托 `self.api`。 |
| `sensor.py` / `button.py` | 平台实体定义。 |
| `history.py` | 日用水历史持久化（见 3.4）。 |

### 3.2 数据源接口（济南水务微信小程序后端）

基础地址 `https://yx.jinanwater.cn`，应用 ID `67532d903748124e443298ca`（见 `const.py`）。

| 端点常量 | 路径 | 用途 |
|---|---|---|
| `API_ENDPOINT` | `/shoufeizjj3/api/WxProgramApi/GetBangDingList` | 账户级数据（绑定户号列表） |
| `API_ENDPOINT_FAPIAO` | `/shoufeizjj3/api/WxProgramApi/GetFaPiaoList?GS=户号` | 账单/用量详情（**返回多次结果，`r1` 是本次抄表日期**） |
| `API_ENDPOINT_GetYiBiaoInfo` | `/shoufeizjj3/api/WxProgramApi/GetYiBiaoInfo?Type=SS` | 实时仪表信息（返回 `jsonData` 数据文件标识） |
| `API_ENDPOINT_GetDataList` | `/shoufeizjj3/api/WxProgramApi/GetDataList` | 日用水明细（需先用 GetYiBiaoInfo 拿文件标识） |

**账单字段映射**（`INVOICE_FIELDS`，取第一条记录）：

```
xzsl / sl  → usage          本期用水量
xzje / zje → current_fee    本期水费
mx         → 明细（mx 数组）
qd         → 本期初始表数
zd         → 本期截止表数
r1         → 本期抄表日期
xzrq       → 本期缴费时间
nlsl       → ⚠️ 原以为是年累计水量，实际不是（见 8. 待办）
```

**日用水记录字段**：`date`（读表日期）、`value`（当日用水量）、`ZhiDu`（止度/表读数）、`value3`（**日均用水量**）。

### 3.3 协调器编排（`JinanWaterCoordinator.async_update_data()`，共 6 步）

```
步骤 1  取账户级数据  fetch_account_list(phone_num)
步骤 2  按 selected_gs 过滤户号
步骤 3  逐户号取账单数据  fetch_invoice_list(gs)   （保留全量，供订单列表用）
步骤 4  把第一条账单的 INVOICE_FIELDS 合并进 merged（保持原有单条行为）
步骤 5  逐户号取日用水  fetch_yibiao_data(gs)
        → await self.history.async_merge(gs, records)   ← 与本地历史合并
        → merged[gs]["yibiao_data"] = records
步骤 6  merged[gs]["order_detail"] = transform_invoice_records(records)
最后    self.last_sync_time = dt_util.utcnow()   ← 自行记录，不依赖 HA 内部属性
```

合并结果结构：`{ gs: { 账户字段..., 账单字段..., "yibiao_data": [...], "order_detail": [...] } }`

### 3.4 日用水持久化（`history.py`）

**为什么需要**：仪表接口只返回「今天往前约 1 个月」的日用水，更早的数据要自己累积。

**落盘位置**（HA 原生 `homeassistant.helpers.storage.Store`，集成数据默认目录）：

```
<HA配置目录>/.storage/jinan_water/daily_history.json
```

相关常量在 `const.py`：`STORAGE_KEY = f"{DOMAIN}/daily_history.json"`（→ `jinan_water/daily_history.json`）、`STORAGE_VERSION = 1`、`MAX_HISTORY_DAYS = 730`。

> 说明：HA 的 `Store` 的 key 支持用 `/` 表示子目录（参考既有项目 github.com/mingyue5826/tongwangas_shandong 的 `Store(hass, 1, f"{DOMAIN}/{mobile}")` 写法）。把 key 设为 `jinan_water/daily_history.json` 即可让 Store 在 `.storage` 下创建 `jinan_water` 子目录并写入 `daily_history.json`，**既用官方 Store、又精确落到子目录 + 带扩展名文件**。

磁盘文件结构（由 Store 自动包裹 `version`）：
```json
{ "version": 1, "data": { "6422376": { "2026-09-03": { "date": "...", "value": 0.43, "ZhiDu": 10.06, "value3": 0.31 } } } }
```

**关键实现要点**：
- 直接用 `Store.async_save()` / `Store.async_load()`，写入由 HA 调度到执行器线程并**原子替换**临时文件，无需自己 `open/os.replace`。
- **旧数据迁移不由代码处理**：若升级前历史写在其它位置（如 `<HA配置目录>/jinan_water/daily_history.json`），请用户手工把数据迁移到新文件（注意 Store 结构为 `{"version":1,"data":{...}}`，旧结构是 `{"version":1,"accounts":{...}}`，迁移时需把顶层 `accounts` 改名为 `data`）。
- **接口数据优先**；但**零值不覆盖已有非零记录**（`_ZERO_GUARD_FIELDS = ("value", "ZhiDu", "value3")`）——因为接口对无数据的日子会返回 0 占位，不能让它把真实历史抹掉。
- 日期归一化为 `YYYY-MM-DD`（剥离接口返回的 `" 00"` 后缀）。
- 按 `MAX_HISTORY_DAYS` 裁剪最旧记录（在内存中完成，`async_save` 一次性落盘）。
- 对外 API：`async_merge(gs, records) -> 合并后的记录列表`、`async_history_count(gs)`。

`sensor.py` 的 `UsageDetailSensor` 与「昨日用水量 / 昨日表读数 / 最新抄表日期」都读合并后的 `YIBIAO_DATA_KEY`，所以曲线会随时间自动变长。

### 3.5 实体清单

统一命名规则：**实体 id = `sensor.jinan_water_<后缀>_<户号>`**，后缀即各类的 `_sensor_key`。

| `_sensor_key`（后缀） | 名称 | 单位 | 说明 |
|---|---|---|---|
| `balance` | 水费余额 | 元 | 账户级 |
| `pending_fee` | 欠费金额 | 元 | 账户级 |
| `penalty_fee` | 违约金 | 元 | 账单级 |
| `order_price` | 本期水价 | 元/m³ | 卡片单价来源 |
| `usage` | 本期用水量 | m³ | 账单 `xzsl`/`sl` |
| `current_fee` | 本期水费 | 元 | 账单 `xzje`/`zje` |
| `yearly_usage` | 年累计用水量 | m³ | ⚠️ 字段待确认，当前恒为 0 |
| `order_meter_reading_start` | 本期初始表数 | m³ | `qd` |
| `order_meter_reading_end` | 本期截止表数 | m³ | `zd` |
| `order_meter_date` | 本期抄表日期 | — | `r1` |
| `order_payment_date` | 本期缴费时间 | — | `xzrq` |
| `address` | 用水地址 | — | 账户级 |
| `username` | 户名 | — | 账户级 |
| `customer_rep` | 客户代表 | — | 账户级 |
| `customer_rep_phone` | 客户代表电话 | — | 账户级 |
| `usage_detail` | 用水详情 | — | **值固定为「图表」，真实日用水列表在 `graph` 属性** |
| `order_detail` | 订单详情 | — | **值固定为「图表」，账单全量列表在 `graph` 属性** |
| `usage_yesterday` | 昨日用水量 | m³ | 日用水最后一条 `value` |
| `meter_yesterday` | 昨日表读数 | m³ | 日用水最后一条 `ZhiDu` |
| `yesterday_meter_reading_date` | 最新抄表日期 | — | 日用水最后一条 `date` |
| `refresh_time` | 上次同步时间 | — | `coordinator.last_sync_time`，`EntityCategory.DIAGNOSTIC` |
| `integration_status` | 集成状态 | — | 诊断实体；状态值「正常/异常」，判断本户号相关全部接口（账户列表+发票列表+仪表数据）是否调用通过；异常时额外信息含 `failed_apis`/`failed_details`/`last_failure_time`，`EntityCategory.DIAGNOSTIC` |

> `failed_details` 每条记录字段：`interface` / `gs` / `error` / `time`(ISO 字符串) / `method`(恒 POST) / `url`(含 query) / `headers`(完整认证头) / `body`(请求体)；无上下文时为 `null`。

**按钮**：`button.jinan_water_refresh_<户号>`，名「刷新数据」，按下触发 `coordinator.async_refresh()`。

> ⚠️ 注意：`usage_detail` 与 `order_detail` 两个传感器的**状态值是固定字符串「图表」，不是数字**；真实数据在 `graph` 属性里（**是真实 Python list，不是 JSON 字符串**）。

### 3.6 配置流 / 选项流踩过的坑

- **`OptionsFlow` 不要手动赋值 `self.config_entry`**（新版 HA 是只读 property，赋值会抛异常，表现为「配置」按钮报 500 且反复重试创建 flow）。需要用到时存到私有属性，本项目用 `self._entry`。
- `JinanWaterOptionsFlow.__init__` 里不调 `super().__init__()`。
- 选项流保存后用 `await self.hass.config_entries.async_reload(entry.entry_id)` 让改动立即生效。
- **HA 版本间内部属性会挪位置**：`DataUpdateCoordinator.last_update_success_time` 在新版被移到了子类 `TimestampDataUpdateCoordinator`。所以本项目**自建 `self.last_sync_time`**，不依赖 HA 内部属性。
- **`Entity.state` / `native_value` 抛异常不会被兜底成 unavailable，而是冒泡**——写实体状态时必须保证不抛异常。

---

## 4. 前端卡片（`custom_components/jinan_water/www/jinan-water-card.js`）

### 4.1 架构

- **零依赖单文件 Web Component**，`customElements.define("jinan-water-card", ...)`。
- **刻意不引图表库**：旧电卡内联了 700KB 的 ApexCharts + Lit，本卡片用**手绘 SVG**（Catmull-Rom 样条转三次贝塞尔 `smoothPath()`），方便维护排错。
- 两个自定义元素：`jinan-water-card`（卡片本体）、`jinan-water-card-editor`（GUI 配置编辑器）。
- HA 卡片接口：`setConfig()` / `set hass` / `static getStubConfig()` / `static getConfigElement()` / `getCardSize()`。
- 每次 `hass` 更新会算 `_dataSignature()`，只有数据真的变了才 `_render()`（避免无谓重绘）。
- 内联 SVG 图标（`ICON_PATHS`），**不依赖 `ha-icon`**，这样预览页也能正常显示。

### 4.2 实体 key → id 后缀映射（`ENTITY_SUFFIX`）

卡片读实体用映射表，不是直接用 key（有几处 key 与后缀不同名）：

| 卡片内部 key | 实际 id 后缀 |
|---|---|
| `balance` | `balance` |
| `pending_fee` | `pending_fee` |
| `penalty_fee` | `penalty_fee` |
| `order_price` | `order_price` |
| `usage` | `usage` |
| `current_fee` | `current_fee` |
| `yearly_usage` | `yearly_usage` |
| `usage_detail` | `usage_detail` |
| `order_detail` | `order_detail` |
| `refresh_time` | `refresh_time` |
| `usage_yesterday` | `usage_yesterday` |
| `meter_yesterday` | `meter_yesterday` |
| `latest_meter_date` | **`yesterday_meter_reading_date`** |
| `order_meter_date` | `order_meter_date` |
| `order_payment_date` | `order_payment_date` |

- **户号自动探测**：`_detectGs()` 用正则 `^sensor\.jinan_water_usage_detail_(.+)$` 取第一个匹配后缀。
- 任意实体可用配置项 `entities: { key: "sensor.xxx" }` 单独覆盖。

> 🕳️ **曾踩的坑**：早期 `ENTITY_SUFFIX` 里存的是中文标签，导致拼出 `sensor.jinan_water_水费余额_6422376` 这种不存在的 id，所有数据读成 null、卡片空白。**改映射表时必须同步核对 `sensor.py` 的 `_sensor_key`。**

### 4.3 界面结构（自上而下）

**主视图**
1. 标题行：`济南水务` + 右侧「同步 xxxx / 数据日期 xxxx」。
2. **三平行指标**：水费余额 / 欠费金额 / 违约金（欠费、违约金 > 0 时标警示色）。
3. **本期用水卡**：
   - 标题左侧 `本期用水`，**右侧小字 `抄表日期 xxxx`**（`.section-sub`）。
   - 左侧：本期用水量 `49 m³` + 本期水费 `¥205.80` + 「对比上期账单 ↑53%」（**hover 弹出上期账单月/抄表日期/用水量/费用**，`.delta` → `.delta-tip`）。
   - 右侧：**sparkline 双折线**（用水量实线 + 日均 `value3` 虚线），**hover 显示读表日期/用水量/日均**；**点击显示与 hover 一致的 tooltip（不再跳转「日用水曲线」面板）**。图例「用水量 / 日均」。
4. **年用水阶梯**：
   - 标题右侧：`**年累计** 0 m³（待接口确认） | ¥491.40`（**只有「年累计」加粗**，其余常规字重）。
   - 第 1 行：3 个**连续**色块（t1 浅蓝 / t2 浅紫 / t3 浅粉），块内 `ladder-tier-fill` 用更深色按进度填充，上方 `ladder-bolt` 闪电图标标记当前位置。
   - 第 2 行：区间标签 `0-96m³ / 97-192m³ / 193m³以上`。
5. 三个按钮：水费日历 / 日用水曲线 / 订单列表（**对应面板默认隐藏**，点击展开，再点收起）。

**面板（默认隐藏）**
- **水费日历**：月历网格，每格显示日用水量与水费（费用 = 用水量 × 单价），可翻月、有「今天」按钮，当天描边高亮。
- **日用水曲线**：独立卡片。**用水量折线 + 水费柱状（半透明紫）+ 日均虚线**；左轴用水量、右轴费用、5 条水平网格线。**hover tooltip 显示：读表日期 / 用水量 / 止度(ZhiDu) / 费用 / 日均**。
- **订单列表**：每笔账单一行（日历图标、月份、金额、已缴/未缴标签），点击展开 mx 明细表（项目 / 用量 / 单价 / 金额）与合计。

### 4.4 配置项

```yaml
type: custom:jinan-water-card
gs: "6422376"          # 户号；不填则自动探测
title: 济南水务
price: 4.20            # 单价覆盖；不填读「本期水价」实体
ladder:                # 年用水阶梯 3 档，不填用默认
  - { name: 第一阶梯, from: 0,   to: 96 }
  - { name: 第二阶梯, from: 97,  to: 192 }
  - { name: 第三阶梯, from: 193, to: null }
default_panel: ""      # "" | "calendar" | "dayChart" | "orders"
entities: {}           # 可选，覆盖任意实体 id
```

- 阶梯也兼容扁平写法 `ladder1_from` / `ladder1_to` / `ladder1_name` …（`parseLadder()` 两种都认）。
- 编辑器 `jinan-water-card-editor` 用 `ha-form` + schema；若 `ha-form` 不可用会**降级为原生 input**（预览页走的就是降级路径）。

### 4.5 供自动化验证用的 `data-*` 属性

- 两张图的 `<svg>` 都带 `data-chart="spark" | "day"` 和 `data-points`（JSON 数组），以及 `data-view-width` / `data-plot-left` / `data-plot-width`，用于把鼠标坐标换算成 viewBox 坐标取最近点。
- 关键类名：`.usage-spark`、`.chart-wrap`、`.chart-tooltip`、`.chart-cursor`、`.ladder-band`、`.ladder-tier`、`.ladder-tier-fill`、`.ladder-bolt`、`.ladder-value`、`.ladder-strong`、`.ladder-money`、`.section-sub`、`.delta`、`.delta-tip`。

---

## 5. 🔬 本地预览与验证方法论（**无图像环境必读**）

> **本机（AI 侧）无法读取图片**（截图会被过滤）。所以所有 UI 验证都走「**Chrome 无头导出 DOM → 提取文本 → 断言**」这条路线。**这是本项目验证前端改动的标准手段。**

### 5.1 预览页的 4 种调试模式

文件：`preview/jinan-water-card-preview.html`（内置 `需求.md` 的真实样例数据作 mock）。

| 模式 | 作用 |
|---|---|
| `?only=default\|calendar\|dayChart\|orders` | 只保留单个视图并放大到 430px，便于聚焦 |
| `?shadow=1` | 把 `card.shadowRoot.innerHTML` 输出到 light-DOM 的 `<pre>`，可脚本化文本断言 |
| `?diag=1` | 输出关键元素的**实际尺寸 / display / 背景色**到 `<pre>` |
| `?hover=1` | **脚本派发人造 `mousemove`**，读 `.chart-tooltip` 的 `hidden`/`textContent`/`left`，再派发 `mouseleave` 验证收起 |

> `?hover=1` 是**无图像环境下验证 hover 的唯一可靠手段**，比截图管用。

### 5.2 无头 Chrome 用法（本机路径）

```bash
CHROME="/c/Program Files/Google/Chrome/Application/chrome.exe"
"$CHROME" --headless=new --no-sandbox --disable-gpu --virtual-time-budget=8000 \
  --dump-dom "file:///E:/workspace/JinanWaterHA/preview/jinan-water-card-preview.html?only=default&shadow=1"
```

`--dump-dom` 会把渲染后的 DOM 打到 stdout，其中 `<pre>` 就是我们注入的诊断文本。用 Python 正则取 `<pre>` 内容即可。

**注意**：`<pre>` 里的内容是被 HTML 转义的（引号变 `&quot;`），提取后需 `html.unescape()` 再解析属性。

### 5.3 环境注意事项

- 本机 Git Bash 的命令环境不稳定（`ls`/`head`/`stat`/`grep` 有时 `command not found`）。**优先用 Python 的 `subprocess` + `os`** 来跑 Chrome、读写文件，比 shell 可靠得多。
- 临时文件请写到 **Windows 路径**（如 `E:\workspace\...\preview\_x.html`），不要用 `/tmp`（Git Bash 的 `/tmp` 与本机 Python 看到的路径不是同一个）。
- 项目根目录**不要留临时脚本**（`_verify.py` 之类用完即删）。
- 静态检查命令：
  ```bash
  node --check jinan-water-card.js                      # 卡片 JS
  python -m py_compile custom_components/jinan_water/*.py   # 后端
  ```

---

## 6. 部署步骤

### 6.1 后端（改动后需**完整重启 HA**，不只是重新加载集成）

把 `custom_components/jinan_water/` 整个目录覆盖到 HA 配置目录，重启。
重启后到 `<HA配置目录>/.storage/jinan_water/` 看是否生成 `daily_history.json`。若升级前历史在 `<HA配置目录>/jinan_water/daily_history.json`，需先手工把数据迁移过来（旧结构是 `{"version":1,"accounts":{...}}`，新结构是 `{"version":1,"data":{...}}`，迁移时把 `accounts` 改名为 `data`）。

### 6.2 前端（**已全自动，不再需要任何手工操作**）

集成启动（`__init__.py` 的 `async_setup`）会调用 `frontend.py` 的 `async_setup_card()`，它做三件事：

| 步骤 | 做法 | 备注 |
|---|---|---|
| ① 暴露 js | `hass.http.async_register_static_paths([StaticPathConfig(...)])` 把 `custom_components/jinan_water/www/jinan-water-card.js` 挂到 `/jinan_water/jinan-water-card.js` | HA 2024.7+ 用 `StaticPathConfig`；旧版回退 `register_static_path()` |
| ② 登记资源 | 把 `/jinan_water/jinan-water-card.js?v=<manifest 版本>` 写进 Lovelace 资源表（`hass.data["lovelace"].resources`），类型 `module` | 等价于用户在「设置 → 仪表盘 → 资源」里手工添加 |
| ③ 兜底 | 资源表不可用（**Lovelace YAML 模式**没有 `store`）→ 用官方公开 API `homeassistant.components.frontend.add_extra_js_url()` 加载 | 该函数 docstring 原文即 "allows custom integrations to register extra js or module" |

用户侧只剩：**重启 HA → 添加卡片 `type: custom:jinan-water-card` → Ctrl+F5**。

**行为细节（已在本地用假 HA 对象跑过全部分支，见 9. 变更历史）**：

- URL 带 `?v=<版本号>`（取自 `manifest.json` 的 `version`）：升级后版本号变化 → URL 变化 → 浏览器丢旧缓存。
- **旧的手工资源自动迁移**：此前手工添加的 `/local/community/jinan-water-card/...`、
  `/local/jinan-water-card.js`、`/hacsfiles/jinan-water-card/...`（见 `const.CARD_LEGACY_URL_PREFIXES`）
  会被改写成新地址；**匹配到多条时只保留一条、其余删除**，避免同一张卡片被加载两遍。
- 已是最新则**不写表**（不开多余的写盘）。
- Lovelace 尚未加载时挂到它的 setup 完成事件上（`async_when_setup`），不阻塞集成启动。
- 卡片文件找不到时只 warning，**不影响集成本体**（会依次找 `www/` 集成自带 → `config/www/jinan_water/` → `config/www/jinan-water-card/` → `config/www/`）。

**卸载清理**（`__init__.py::async_remove_entry` → `frontend.py::async_remove_card`）：

- HA 调用 `async_remove_entry` 时该 entry **已经从 `hass.config_entries` 摘掉**（core 的
  `ConfigEntries._async_remove` 先 `del self._entries[...]` 再 `await entry.async_remove()`），
  所以 `hass.config_entries.async_entries(DOMAIN)` 为空 == 最后一个条目 → 执行移除。
- 多账号场景下删其中一个条目，资源保留；全删完才清理。
- 只删文件名是 `jinan-water-card.js` 且属于本命名空间的那几条，**不碰别的卡片**（已本地验证）。
- ⚠️ **覆盖不到的场景**：如果先用 HACS 删文件、没先在 HA 里删条目，重启后 HA 加载不到集成
  （`loader.async_get_integration` 抛 `IntegrationNotFound` 时 core 直接 `return`），**清理钩子不会执行**，
  此时只能手工删资源。已在 README 里写明「先在 HA 删条目，再删文件」。
- 参考对比：**WebRTC 没有这个清理**（它的 `async_setup` 只注册，`async_unload_entry` 只停 go2rtc，
  卸载后资源会残留）；只有 **HACS 对 plugin** 做了 `remove_dashboard_resources()`。本实现向 HACS 看齐。

> 🕳️ **仍可能报 `Custom element doesn't exist: jinan-water-card` 的原因只剩两个**：
> ① 浏览器缓存（Ctrl+F5）；② `www/jinan-water-card.js` 没被部署过去（HACS 没下载 / 手动安装漏拷 `www/`）。
> 排查：浏览器直接打开 `http://<HA地址>/jinan_water/jinan-water-card.js`，**能下载到 js 就说明注册成功**。

### 6.3 发布到 HACS 时的注意事项

#### 现状：`hacs.json` 已开启 `zip_release`

```json
"zip_release": true,
"filename": "jinan_water.zip"
```

开启后 HACS **下载 release 里的 `jinan_water.zip` 并 `extractall` 到 `<config>/custom_components/jinan_water/`**。
由此推出三条硬约束（改打包命令前必须看）：

1. **zip 内部必须"平铺"**，根目录直接是 `manifest.json` / `__init__.py` / `www/…`。
   如果还像以前那样带 `jinan_water/` 顶层目录，解压后会变成
   `<config>/custom_components/jinan_water/jinan_water/manifest.json`，**集成直接加载不出来**。
2. `filename` 必须存在且以 `.zip` 结尾，否则 HACS 根本不进 zip 分支（`base.py::should_try_releases`）。
3. 只有装 **某个 release/tag 版本** 时才走 zip（源码里 `self.ref != default_branch` 的判断）；
   用户若选安装默认分支，HACS 仍逐文件下载仓库 tree → 所以 workflow 末尾还有一步
   **`Sync manifest version to default branch`** 把版本提交回去兜底。

#### Release 产出两个 zip，用途不同，别混

| 文件 | 结构 | 给谁用 |
|---|---|---|
| `jinan_water.zip` | 平铺（无顶层目录） | **HACS**（`hacs.json` 的 `filename` 指向它） |
| `jinan_water-manual.zip` | 带 `jinan_water/` 顶层目录 | **手动安装**的用户，解压丢进 `custom_components/` |

#### 打 tag 前必须让 manifest version 与 tag 对齐（强制校验）

- `release.yml` 的 **`Verify tag matches manifest version`** 步会强制校验：tag 名（忽略开头 `v`/`V`/`-`）
  必须等于 `manifest.json` 的 `version`，**否则直接失败、不产出 release**。
- 另有独立 workflow **`tag-check.yml`**，只要打 `v*` tag 就跑同一套校验，给开发者即时红 X 反馈
  （`on: push: tags: ["v*"]`，权限仅 `contents: read`）。
- 归一化规则：`re.sub(r"^[vV-]+", "", tag)`，即 `v1.0.0` / `V-1.0.0` / `-1.0.0` 都视作 `1.0.0`。
  manifest 的 version 不要带前缀（HA 约定纯版本号）。

**正确发版顺序**：

1. 本地把 `manifest.json` 的 `version` 改成要发的版本（如 `1.1.0`）；
2. `git commit -am "release: 1.1.0" && git push`；
3. `git tag v1.1.0 && git push --tags` → 校验通过 → 自动打包发布。

> ⚠️ 注意：**不再是"打 tag 即发版、CI 替你改版本"**。现在改成严格门禁，
> manifest 与 tag 不一致时发布会被拦下来——这能避免发出版本号错乱的 release。

CI 里还有一道**打包完整性校验**（失败会阻断发版）：

- `jinan_water.zip`：必须有根级 `manifest.json`、`__init__.py`、`www/jinan-water-card.js`；
  **不能有** `jinan_water/` 前缀；不能有 `__pycache__` / `.pyc`；包内版本须等于 tag 版本。
- `jinan_water-manual.zip`：必须有 `jinan_water/manifest.json`、`jinan_water/www/jinan-water-card.js`。

> 🕳️ **仍建议本地把 manifest 版本一起提交**：HACS 在 `validate_repository()` 阶段仍会读仓库 tree 的
> manifest 来判定 domain，两者差太多容易混淆。workflow 末尾已自动补开一个 `[skip ci]` 提交同步版本。
> （仅当默认分支落后于 tag 提交时才真正提交；已对齐则跳过。）

---

## 7. 🕳️ 踩坑清单（血泪，改代码前先看）

| # | 坑 | 结论 |
|---|---|---|
| 1 | **SVG hover 完全失效** | `_onMouseMove` 曾用 `node instanceof HTMLElement` 过滤 `composedPath()` 找图表——但图表本体是 `<svg>`，属于 **`SVGElement` 不是 `HTMLElement`**，条件永远 false → hover 全废。**改用 `node.dataset.chart` 鸭子判断**。教训：Web Component 里给 SVG 挂交互，**绝不要用 `instanceof HTMLElement` 过滤**；用 `dataset` / `closest()`。 |
| 2 | `mouseleave` 不触发 | `mouseleave` **不冒泡**，挂在 `shadowRoot` 上不可靠。**挂宿主元素 `this`**。（`mousemove` 冒泡，挂 shadowRoot 没问题。） |
| 3 | 实体读不到 → 卡片空白 | `ENTITY_SUFFIX` 必须存**真实 `_sensor_key` 后缀**，不能存中文标签。 |
| 4 | `Custom element doesn't exist` | 早期是资源 URL 少写目录层级；**0.2.0 起由集成自动注册**，只剩「浏览器缓存」和「www/ 没部署过去」两种可能（见 6.2）。 |
| 12 | HACS 装完卡片用不了 | HACS **只对 plugin 类型仓库**自动登记 Lovelace 资源，integration 类型一概不管。所以集成必须自己在 `async_setup` 里注册（方案与 AlexxIT/WebRTC 一致）。 |
| 5 | 「配置」按钮 500 | `OptionsFlow` 不能手动赋值 `self.config_entry`（只读属性）。 |
| 6 | 「上次同步时间」不可用 | 不同 HA 版本 `last_update_success_time` 所在位置不同 → 自建 `self.last_sync_time`。 |
| 7 | 按钮「自动被按下」 | HA `ButtonEntity` 继承 `RestoreEntity`，重载后会恢复上次状态显示——**并非真的执行了 `async_press`**，属内置行为，无需改。 |
| 8 | 日曲线「费用曲线与用水量曲线完全重合」 | 因为 `费用 = 用水量 × 单价`，两条线数学上同形。**改成：费用用半透明柱 + 用水量用线**。 |
| 9 | 接口 0 值抹掉历史 | 接口对无数据的日子返回 0 占位 → 持久化时加**零值守卫**，不覆盖已有非零记录。 |
| 10 | `<b>` 加粗在部分字体下粗细飘 | `<b>` 的 UA 默认是 `font-weight: bolder` 而非确定值，**显式写 `700`**。 |
| 11 | 本机 shell 不稳 | Git Bash 的 `ls`/`head`/`grep` 时常 `command not found`，优先用 Python 脚本替代。 |

---

## 8. 已知待办 / 未决问题

| 项 | 状态 | 说明 |
|---|---|---|
| **年累计用水量字段** | ⏳ **待用户确认** | `nlsl` 实际**并非**年累计水量。`YearlyUsageSensor` 目前无正确数据源、恒为 0，卡片阶梯显示「年累计 0 m³（待接口确认）」，进度条填充宽度为 0。**用户已决策：阶梯只读「年累计用水量」实体**，等字段确认后即可自动生效。 |
| 阶梯色块饱和度 | 观察中 | 用户已看过，暂无反馈；如需调整改 `.ladder-tier.t1/t2/t3` 与 `.ladder-tier-fill` 的色值。 |
| `preview/*.png` | 可清理 | 早期调试截图（`diag.png` / `shot.png` / `v-*.png`），已无用，可删。 |
| 其他户号 | 未验证 | 目前只在 1 个户号（`6422376`）上验证过；多户号场景的实体范围需再确认。 |

---

## 9. 变更历史（近期）

| 日期 | 版本 | 内容 |
|---|---|---|
| 2026-09-13 | — | 后端重构：抽出 `api.py` / `helpers.py`，`__init__.py` 精简为编排；新增「上次同步时间」实体 |
| 2026-09-13 | — | 修复「配置」按钮 500（`OptionsFlow.config_entry` 冲突）；修复 `last_update_success_time` 不可用 |
| 2026-09-13 | — | 新增前端卡片 `custom_components/jinan_water/www/jinan-water-card.js`（零依赖 Web Component）+ 本地预览页 |
| 2026-09-13 | 1.0.0 | 卡片首发：三平行指标 / 本期用水 / 年阶梯 / 日历 / 日曲线 / 订单列表 |
| 2026-09-13 | — | 新增 `history.py` 日用水持久化（初版用 HA `Store` 写 `.storage`） |
| 2026-09-13 | — | 持久化改为写可见文件 `<config>/jinan_water/daily_history.json` |
| 2026-09-13 | 1.1.0 | 卡片 5 处调整：双折线 hover、年阶梯改 2 行色块、日曲线 tooltip 加止度、删「近30天日均/单价」、对比上期账单加 hover |
| 2026-09-13 | **1.1.1** | **修复两张图 hover 完全失效（SVGElement 坑）**；本期用水加「抄表日期」小字；删除「上期账单」「本年（账单合计）」两张独立小卡，本年金额挪到阶梯标题右侧 |
| 2026-09-13 | **1.1.2** | 阶梯标题文案加粗范围收窄（仅「年累计」加粗）；删除根目录临时脚本 `_verify.py` |
| 2026-09-14 | **1.1.3** | 本期用水平面 spark：取消「点击打开日用水曲线」面板，改为点击显示与 hover 一致的数据 tooltip（抽公共方法 `_showTooltipForChart`，hover/点击共用） |
| 2026-09-19 | **0.2.0** | **卡片自动注册**：新增 `frontend.py`（静态路径 + 写 Lovelace 资源表 + `add_extra_js_url` 兜底 + 旧手工资源迁移/去重），部署不再需要手工复制 js、手工添加资源 URL；`const.py` 增 `CARD_*` 常量 |
| 2026-09-19 | **1.0.0** | 新增**卸载自动移除资源**（`async_remove_entry` → `async_remove_card`，最后一个条目删完才清理，不误伤其它卡片）；`release.yml` 加版本一致性与打包完整性校验（缺 `www/jinan-water-card.js` 直接失败） |
| 2026-09-19 | **1.0.1** | **发版彻底告别手改版本号**：`hacs.json` 开 `zip_release` + `filename`；`release.yml` 改为产出两个包（`jinan_water.zip` 平铺给 HACS / `jinan_water-manual.zip` 带目录给手动安装）、按 tag 自动改写 manifest 版本、末步把版本同步回默认分支（兜底 HACS 装分支的场景） |
| 2026-09-19 | — | 新增「集成状态」诊断实体（`integration_status`，每户号一个）：覆盖全部接口（账户列表+各户号发票列表+各户号仪表数据），任一失败则状态=异常并在额外信息列失败接口清单/明细/时间；接口吞错处（`fetch_invoice_list`/`fetch_yibiao_data`）改为"记录失败仍返回空"；账户列表失败不再整体抛错，改为记录并继续 |
| 2026-09-19 | — | 「集成状态」失败明细增强：`failed_details` 每条额外记录**请求详情** `method`/`url`/`headers`/`body`（`_call_api` 把请求上下文挂在异常 `error.request` 上，`_record_failure` 取出；仪表接口两步串行时回退到实际发出的最后一个请求）。同时修一批运行时 bug：`_record_failure`/`self.failures` 此前只写了调用点未真正定义（`py_compile` 查不出，运行必崩）、`fetch_invoice_list` 漏记失败、账户列表仍整体抛错、失败记录跨轮不清空 |
| 2026-09-15 | — | 持久化落盘位置改为 `<config>/.storage/jinan_water/daily_history.json`（HA 的 `Store` 不支持子目录 + 扩展名，故用 `hass.config.path` + 执行器线程 + 原子替换写入 `.storage`），并新增旧文件一次性迁移逻辑 |

---

## 10. 新窗口接手 checklist

1. 读 `需求.md`（约定优先）+ 本文档。
2. 读 `.workbuddy/memory/MEMORY.md` 和最近的 `.workbuddy/memory/YYYY-MM-DD.md`（有更细的历史日志）。
3. 改后端后：`python -m py_compile custom_components/jinan_water/*.py`。
4**有不确定的地方先问用户**，不要猜。
